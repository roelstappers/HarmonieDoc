N[type=="ecflowtask"] { 
  
  if ($.script == "")  $.script=$.name;
  
  SCRDIR="./scr/";
  fid = openF(SCRDIR + $.script,"r");
  docstr = readL(fid);
  while (substr(docstr,0,1) == "#") { 
    $.tooltip  = $.tooltip + "&#13;&#10;" + docstr;
    docstr = readL(fid);
  }
  closeF(fid);
} 
